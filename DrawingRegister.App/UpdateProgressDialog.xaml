<Window x:Class="DrawingRegister.App.UpdateProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Downloading Update"
        Height="160" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White" WindowStyle="None" AllowsTransparency="True">
    <Border BorderBrush="{StaticResource Slate200Brush}" BorderThickness="1" CornerRadius="10" Background="White"
            Effect="{StaticResource ElevatedShadow}" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="20,14,20,10" MouseLeftButtonDown="Header_DragMove">
                <TextBlock Text="Downloading Update" Style="{StaticResource HeadingMedium}"/>
            </Grid>

            <!-- Accent Line -->
            <Border Grid.Row="1" Background="{StaticResource BrandRedBrush}" Height="3"/>

            <!-- Content -->
            <StackPanel Grid.Row="2" Margin="20,16,20,20">
                <TextBlock Text="Downloading update, please wait..."
                           FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,12"/>

                <ProgressBar x:Name="ProgressBar" Height="6" Minimum="0" Maximum="100" Value="0"/>

                <TextBlock x:Name="ProgressText" Text="0%" HorizontalAlignment="Center"
                           FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,8,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
