<Window x:Class="DrawingRegister.App.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Document and Drawing Register"
        Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        Background="White">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Company Logo -->
        <Image Grid.Row="0"
               Source="pack://application:,,,/DrawingRegister.App;component/Resources/company-logo.png"
               Height="60"
               HorizontalAlignment="Center"
               Margin="0,0,0,20"/>

        <!-- Application Title -->
        <TextBlock Grid.Row="1"
                   Text="Document and Drawing Register"
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Foreground="#eb1845"
                   Margin="0,0,0,10"/>

        <!-- Version -->
        <TextBlock x:Name="VersionText"
                   Grid.Row="2"
                   Text="Version 1.0.0"
                   FontSize="14"
                   HorizontalAlignment="Center"
                   Foreground="#666666"
                   Margin="0,0,0,20"/>

        <!-- Description -->
        <TextBlock Grid.Row="3"
                   Text="A professional tool for managing document and drawing registers for engineering projects."
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   FontSize="12"
                   Foreground="#444444"
                   Margin="0,0,0,20"/>

        <!-- GitHub Link -->
        <TextBlock Grid.Row="4"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20">
            <Hyperlink x:Name="GitHubLink"
                       NavigateUri="https://github.com/kyle93afc/drawingregisterMJ"
                       RequestNavigate="Hyperlink_RequestNavigate">
                View on GitHub
            </Hyperlink>
        </TextBlock>

        <!-- Copyright -->
        <TextBlock Grid.Row="5"
                   Text="Copyright (c) 2025 M+J Engineers"
                   FontSize="11"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   Foreground="#888888"
                   Margin="0,0,0,20"/>

        <!-- Close Button -->
        <Button Grid.Row="6"
                Content="Close"
                Width="100"
                Height="30"
                HorizontalAlignment="Center"
                Click="Close_Click"
                Background="#eb1845"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand"/>
    </Grid>
</Window>
