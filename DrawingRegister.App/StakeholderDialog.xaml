<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="DrawingRegister.App.StakeholderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DrawingRegister.App"
        mc:Ignorable="d"
        Title="Stakeholder Details" Height="260" Width="420"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Slate50Brush}">
    <Grid Margin="16">
        <Border Style="{StaticResource CardStyle}" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <TextBlock Grid.Row="0" Text="Stakeholder Details" Style="{StaticResource HeadingMedium}" Margin="0,0,0,16"/>

                <!-- Name -->
                <StackPanel Grid.Row="1" Margin="0,0,0,12">
                    <TextBlock Text="Name" Style="{StaticResource SectionLabel}" Margin="0,0,0,4"/>
                    <TextBox x:Name="NameTextBox" Height="30"/>
                </StackPanel>

                <!-- Company -->
                <StackPanel Grid.Row="2" Margin="0,0,0,12">
                    <TextBlock Text="Company" Style="{StaticResource SectionLabel}" Margin="0,0,0,4"/>
                    <TextBox x:Name="CompanyTextBox" Height="30"/>
                </StackPanel>

                <!-- Buttons -->
                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelButton" Content="Cancel" Click="Cancel_Click"
                            Style="{StaticResource GhostButton}" Height="32" Margin="0,0,8,0"/>
                    <Button x:Name="SaveButton" Content="Save" Click="Save_Click"
                            Style="{StaticResource PrimaryButton}" Height="32"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
